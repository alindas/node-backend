const Log = require("./log.decorator");
const LoginLog = require("./loginLog.decorator");
const HasPermissions = require("./permissions.decorator");

module.exports = {
  Log,
  LoginLog,
  HasPermissions,
};
